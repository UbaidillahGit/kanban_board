import 'package:flutter/material.dart';

class GlobalConstants {
    static const Color canvasColor = Color.fromRGBO(231, 232, 236, 10);
    static const Color mainThemeColor = Colors.blueGrey;
    static const Size cardSize = Size(400, 400);
}